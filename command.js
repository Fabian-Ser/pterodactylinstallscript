const discord = require("discord.js");

module.exports = {
    config: {
        name: "COMMANDNAMEHERE",
        aliases: []
    },
    run: async(bot, message, args) => {
    
        var starthere = null
        
    }
}
